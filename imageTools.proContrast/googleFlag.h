#pragma once
#include <gflags/gflags.h>

DEFINE_string(inputImage, "", "The intput source image.");
DEFINE_string(outputImage, "pro-contrast-image", "The output image.");
DEFINE_string(format, "png", "The output image format, default is png.");
